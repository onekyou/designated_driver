
    private val callStatusUpdateReceiver = object : BroadcastReceiver() {
        override fun onReceive(context: Context?, intent: Intent?) {
            if (intent?.action == "com.designated.callmanager.CALL_STATUS_UPDATE") {
                val callId = intent.getStringExtra("callId")
                val status = intent.getStringExtra("status")
                Log.d("MainActivity", "📢 CALL_STATUS_UPDATE 수신: callId=$callId, status=$status")
                // Firestore의 자동 동기화가 local cache를 업데이트합니다
            }
        }
    }
