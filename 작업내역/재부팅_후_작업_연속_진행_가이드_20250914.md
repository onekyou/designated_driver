# 재부팅 후 작업 연속 진행 가이드

**작성일**: 2025-01-14
**상태**: 작업 준비 완료 - FCM 토큰 관리 및 알림 시스템 수정 대기 중
**진행률**: FCM 토큰 관리 수정 작업 준비 완료

## 현재까지 확인된 상황

### 1. 올바른 프로젝트 경로 확인 완료
- **올바른 백업 경로**: `C:/app_dev/designated_driver/call_manager_backup`
- **잘못된 경로**: `C:\Users\onekyou\Desktop\final\call_manager` (여기서 잘못 작업했음)
- **백업 특징**: `call_manager_no_ptt.iml` 파일 존재 → PTT 제거된 버전

### 2. FCM 알림 문제 원인 파악 완료

#### 주요 문제들:
1. **FCM 토큰 관리 부족**
   - 앱 시작 시 토큰 동기화 로직 없음
   - Functions에서 "⚠️ FCM 토큰 없음: office_1754905384849" 오류 발생

2. **포그라운드 알림 차단**
   - MyFirebaseMessagingService의 43번째 줄:
   ```kotlin
   val shouldProcessInForeground = messageType == "SHARED_CALL_CANCELLED_POPUP"
   ```
   - `NEW_SHARED_CALL`이 포함되지 않아 포그라운드에서 알림 무시됨

3. **디버깅 불가능 상태**
   - MyFirebaseMessagingService에 로그가 전혀 없음
   - onCreate, onMessageReceived 등에 디버깅 로그 부재

### 3. PTT 충돌 문제 없음 확인
- ✅ 백업에 PTT 관련 파일 없음
- ✅ AndroidManifest에 PTT 서비스 등록 없음
- ✅ FCM 서비스는 MyFirebaseMessagingService만 등록됨

## 해결해야 할 작업 목록

### 우선순위 1: FCM 토큰 관리 수정 (진행 중 중단)
**파일**: `C:/app_dev/designated_driver/call_manager_backup/app/src/main/java/com/designated/callmanager/service/MyFirebaseMessagingService.kt`

#### 1-1. onCreate() 함수 수정 (Edit 도구 오류로 중단)
**위치**: 33-36번째 줄
```kotlin
// 현재 상태
override fun onCreate() {
    super.onCreate()
    createNotificationChannels()
}

// 수정 필요
override fun onCreate() {
    super.onCreate()
    createNotificationChannels()

    // 서비스 시작 시 기존 토큰 확인 및 동기화
    checkAndSyncExistingToken()
}
```

#### 1-2. checkAndSyncExistingToken() 함수 추가 필요
**위치**: onCreate() 함수 이후에 추가
```kotlin
// 기존 토큰 확인 및 Firestore 동기화 (정석적 방법)
private fun checkAndSyncExistingToken() {
    // 현재 유효한 토큰 가져오기 (캐시된 토큰 사용)
    com.google.firebase.messaging.FirebaseMessaging.getInstance().token
        .addOnCompleteListener { task ->
            if (!task.isSuccessful) {
                return@addOnCompleteListener
            }

            val currentToken = task.result

            // SharedPreferences의 토큰과 비교
            val sharedPrefs = getSharedPreferences("fcm_prefs", Context.MODE_PRIVATE)
            val savedToken = sharedPrefs.getString("fcm_token", null)

            when {
                savedToken == null -> {
                    // SharedPreferences에 토큰 없음 - 새로 저장
                    sharedPrefs.edit().putString("fcm_token", currentToken).apply()
                    saveTokenToFirestore(currentToken)
                }
                savedToken != currentToken -> {
                    // 토큰 변경됨 - 업데이트 필요
                    sharedPrefs.edit().putString("fcm_token", currentToken).apply()
                    saveTokenToFirestore(currentToken)
                }
                else -> {
                    // 토큰은 같지만 Firestore에 저장되어 있는지 확인
                    verifyTokenInFirestore(currentToken)
                }
            }
        }
}
```

#### 1-3. verifyTokenInFirestore() 함수 추가 필요
```kotlin
// Firestore에 토큰이 저장되어 있는지 확인
private fun verifyTokenInFirestore(token: String) {
    val auth = FirebaseAuth.getInstance()
    val currentUser = auth.currentUser

    if (currentUser == null) {
        return
    }

    val firestore = FirebaseFirestore.getInstance()
    firestore.collection("admins").document(currentUser.uid)
        .get()
        .addOnSuccessListener { document ->
            val firestoreToken = document.getString("fcmToken")
            if (firestoreToken != token) {
                // Firestore 토큰 불일치 - 업데이트 필요
                saveTokenToFirestore(token)
            }
        }
        .addOnFailureListener {
            // Firestore 토큰 확인 실패 - 재저장 시도
            saveTokenToFirestore(token)
        }
}
```

### 우선순위 2: 디버깅 로그 추가
**파일**: 동일한 MyFirebaseMessagingService.kt

#### 2-1. onCreate()에 로그 추가
```kotlin
override fun onCreate() {
    super.onCreate()
    Log.d(TAG, "========== MyFirebaseMessagingService onCreate 호출됨 ==========")

    createNotificationChannels()
    checkAndSyncExistingToken()

    Log.d(TAG, "========== MyFirebaseMessagingService onCreate 완료 ==========")
}
```

#### 2-2. onMessageReceived()에 로그 추가
**위치**: 38번째 줄 이후
```kotlin
override fun onMessageReceived(remoteMessage: RemoteMessage) {
    super.onMessageReceived(remoteMessage)

    // 기본 진입점 확인 로그
    Log.d(TAG, "========== FCM 메시지 수신됨 ==========")
    Log.d(TAG, "🔔 FCM 메시지 수신: ${remoteMessage.from}")
    Log.d(TAG, "데이터: ${remoteMessage.data}")
    Log.d(TAG, "알림: ${remoteMessage.notification}")
    Log.d(TAG, "==================================")

    val messageType = remoteMessage.data["type"] ?: return
    // ... 기존 코드 ...
}
```

### 우선순위 3: 포그라운드 알림 수정
**파일**: 동일한 MyFirebaseMessagingService.kt
**위치**: 43번째 줄

```kotlin
// 현재 문제 코드
val shouldProcessInForeground = messageType == "SHARED_CALL_CANCELLED_POPUP"

// 수정 필요
val shouldProcessInForeground = messageType == "SHARED_CALL_CANCELLED_POPUP" ||
                               messageType == "NEW_SHARED_CALL"
```

## 이전 세션 작업 기록

### Edit 도구 관련 이슈 (해결됨)
- **이전 오류**: "File has been unexpectedly modified" 오류 발생
- **원인**: 시스템 파일 락 또는 캐시 이슈로 추정
- **해결**: 시스템 재부팅 후 정상 작동 예상

## 재부팅 후 진행 방법

### 1단계: Edit 도구 정상 작동 확인
```
간단한 테스트 파일로 Edit 도구 동작 확인
```

### 2단계: FCM 토큰 관리 완성
- 위의 1-1, 1-2, 1-3 코드를 MyFirebaseMessagingService.kt에 추가
- 테스트 및 확인

### 3단계: 디버깅 로그 추가
- 위의 2-1, 2-2 로그 코드 추가
- 앱 실행하여 로그 확인

### 4단계: 포그라운드 알림 수정
- 위의 우선순위 3 코드 수정
- 포그라운드에서 공유콜 알림 테스트

### 5단계: 종합 테스트
- Firebase Functions 로그에서 "FCM 토큰 없음" 오류 해결 확인
- 포그라운드/백그라운드 모든 상황에서 공유콜 알림 정상 작동 확인

## 중요 파일 경로
- **백업 프로젝트**: `C:/app_dev/designated_driver/call_manager_backup`
- **수정 대상**: `app/src/main/java/com/designated/callmanager/service/MyFirebaseMessagingService.kt`
- **Firebase Functions**: `C:/app_dev/designated_driver/functions/src/index.ts` (수정 완료됨)

## 주의사항
1. **정석적 토큰 관리**: 강제 갱신이 아닌 동기화 방식 사용
2. **하나씩 작업**: 한 번에 하나의 수정사항만 적용하고 테스트
3. **로그 우선**: 디버깅 로그부터 추가하여 문제 상황 파악 가능하게 만들기

---

**다음 세션에서 이 문서를 참조하여 Edit 도구 테스트부터 시작하세요.**