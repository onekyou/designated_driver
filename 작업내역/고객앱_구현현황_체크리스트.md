# 🎯 고객앱 구현 현황 체크리스트

## 📅 체크 기준일: 2024.12 (수정된 계획서 기준)

---

## Week 1: 기본 구조

### ✅ 완료된 항목
- [x] **Android 프로젝트 생성 및 설정**
  - `customer_app/app/` 프로젝트 구조 완성
  - Jetpack Compose 설정 완료
  - 기본 Theme, Color, Type 구성

- [x] **Firebase 프로젝트 연동**
  - `functions/` Firebase Cloud Functions 프로젝트 생성
  - Firebase 설정 파일 연동

- [x] **QR 스캔 기능 구현**
  - `QrScanScreen.kt` - QR 스캔 화면 구현
  - `QrScanViewModel.kt` - QR 스캔 로직 구현
  - `QrOfficeData.kt` - QR 데이터 모델 구현

- [x] **Phone Auth 구현**
  - `PhoneAuthScreen.kt` - 전화번호 인증 화면
  - `PhoneAuthViewModel.kt` - 인증 로직 구현

### ❌ 수정 필요 항목
- [ ] **QR 데이터 구조 수정 필요**
  - 현재: JSON 형식 `QrOfficeData` 클래스
  - 필요: URL 파싱 `https://designated-driver.app/office/{regionId}/{officeId}`

- [ ] **사무실 연결 로직 수정**
  - 현재: 복잡한 Attribution 시스템
  - 필요: 단순 SharedPreferences 저장

---

## Week 2: 핵심 기능

### ✅ 완료된 항목
- [x] **메인 화면 구조**
  - `MainScreen.kt` - 메인 화면 레이아웃 완성
  - `MainViewModel.kt` - 메인 로직 구현
  - 위치 입력 UI 구현

- [x] **위치 서비스 연동**
  - `LocationService.kt` - GPS 위치 서비스 구현

- [x] **콜 생성 기본 구조**
  - `CallService.kt` - 콜 생성 서비스 구현
  - `CustomerCall.kt` - 콜 데이터 모델

### ❌ 수정 필요 항목
- [ ] **콜 생성 데이터 구조 수정 필요** ⚠️ 중요!
  - 현재: `customerCalls` 컬렉션 사용
  - 필요: `calls` 컬렉션으로 변경
  - 추가 필요 필드:
    ```kotlin
    createdFrom: "customer_app"
    customerId: string
    customerGrade: string
    isAppCustomer: true
    attributionScore: number (선택)
    ```

- [ ] **실시간 콜 상태 모니터링 수정**
  - 현재: `customerCalls` 컬렉션 리스닝
  - 필요: `calls` 컬렉션 리스닝으로 변경

---

## Week 3: 포인트 시스템

### ❌ 미구현 항목
- [ ] **포인트 적립 로직** - 미구현
- [ ] **등급 시스템 구현** - 미구현
- [ ] **포인트 조회 기능** - 미구현
- [ ] **이용 내역 화면** - 미구현

---

## Week 4: 테스트 및 배포

### ❌ 미구현 항목
- [ ] **통합 테스트 (콜매니저 연동)** - 미구현
- [ ] **성능 최적화** - 미구현
- [ ] **버그 수정** - 미구현
- [ ] **Play Store 등록 준비** - 미구현

---

## 📋 추가 구현된 항목 (계획서에 없던 것)

### ✅ 과도한 구현 (제거 대상)
- [x] **복잡한 어트리뷰션 시스템** ⚠️ 제거 필요
  - `AttributionService.kt` - 70점 임계값 시스템
  - `AttributionResult.kt` - 복잡한 매칭 결과
  - `DeviceFingerprint.kt` - 핑거프린팅 시스템
  - `FingerprintManager.kt` - 핑거프린팅 관리자
  - `functions/src/index.ts` - 복잡한 Cloud Functions

- [x] **랜딩페이지 구현** ⚠️ 단순화 필요
  - `landing/app/[officeCode]/page.tsx` - 핑거프린팅 포함
  - 필요: 단순한 앱 다운로드 페이지로 변경

---

## 🚨 긴급 수정 필요 항목

### 1. **CallService.kt 수정 (최우선)**
```kotlin
// 현재 (잘못됨)
.collection("customerCalls")

// 수정 필요
.collection("calls")

// 추가 필요 필드
"createdFrom" to "customer_app"
"isAppCustomer" to true
```

### 2. **QR 파싱 로직 수정**
```kotlin
// 현재: JSON 파싱
QrOfficeData.fromJson(qrText)

// 수정 필요: URL 파싱
// https://designated-driver.app/office/{regionId}/{officeId}
```

### 3. **어트리뷰션 시스템 제거**
- 복잡한 핑거프린팅 관련 파일들 제거
- 단순 SharedPreferences 저장으로 변경

---

## 📊 전체 진행률

| 카테고리 | 완료 | 수정필요 | 미구현 | 진행률 |
|---------|------|----------|--------|--------|
| **기본 구조** | 4개 | 2개 | 0개 | 67% |
| **핵심 기능** | 3개 | 2개 | 0개 | 60% |
| **포인트 시스템** | 0개 | 0개 | 4개 | 0% |
| **테스트/배포** | 0개 | 0개 | 4개 | 0% |
| **전체** | 7개 | 4개 | 8개 | **37%** |

---

## 🎯 다음 챗에서 우선 작업

### 1순위: 콜매니저 호환성 수정
1. `CallService.kt` - 컬렉션명 및 필드 수정
2. `CustomerCall.kt` - 데이터 모델 필드 추가
3. QR 파싱 로직 URL 방식으로 변경

### 2순위: 불필요한 코드 제거
1. 어트리뷰션 관련 파일들 제거
2. 핑거프린팅 시스템 제거
3. 복잡한 Cloud Functions 단순화

### 3순위: 누락 기능 구현
1. 포인트 시스템 구현
2. 이용내역 화면 구현
3. 통합 테스트

---

## ✅ 결론

**현재 상태**: 기본 구조는 완성되었지만 **콜매니저와 호환성 문제** 존재

**긴급 조치**: 데이터 구조 수정 후 곧바로 연동 테스트 가능

**예상 완성**: 호환성 수정 후 2-3주 내 MVP 완성 가능