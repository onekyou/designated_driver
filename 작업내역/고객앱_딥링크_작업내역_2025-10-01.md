# 고객앱 딥링크 시스템 작업내역 (2025-10-01)

## 완료된 작업

### 1. AndroidManifest.xml - 딥링크 intent-filter 추가
**파일:** `customer_app/app/app/src/main/AndroidManifest.xml`

```xml
<activity
    android:name=".MainActivity"
    android:exported="true"
    android:launchMode="singleTask">

    <!-- 딥링크 처리를 위한 intent-filter -->
    <intent-filter android:autoVerify="true">
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data
            android:scheme="designatedcustomer"
            android:host="open" />
    </intent-filter>
</activity>
```

### 2. MainActivity.kt - 개발자 모드 제거 및 로깅 강화
**파일:** `customer_app/app/app/src/main/java/com/designated/customer/MainActivity.kt`

**변경사항:**
- ❌ 개발자 모드 코드 완전 제거 (office1 테스트 데이터 자동 설정)
- ✅ 상세한 딥링크 디버깅 로그 추가
- ✅ 딥링크 파라미터 추출 및 SharedPreferences 저장

**핵심 로직:**
```kotlin
private fun handleDeepLink(intent: Intent) {
    android.util.Log.d("MainActivity", "========== handleDeepLink 시작 ==========")
    android.util.Log.d("MainActivity", "Intent Action: ${intent.action}")
    android.util.Log.d("MainActivity", "Intent Data: ${intent.data}")

    val data: Uri? = intent.data
    val prefs = PreferencesManager(this)

    if (data != null) {
        val regionId = data.getQueryParameter("r")
        val officeId = data.getQueryParameter("o")
        val officePhone = data.getQueryParameter("phone")
        val bankName = data.getQueryParameter("bank")
        val accountNumber = data.getQueryParameter("account")
        val accountHolder = data.getQueryParameter("holder")

        // 로그 출력
        android.util.Log.d("MainActivity", "추출된 파라미터:")
        android.util.Log.d("MainActivity", "  - regionId: $regionId")
        android.util.Log.d("MainActivity", "  - officeId: $officeId")
        android.util.Log.d("MainActivity", "  - phone: $officePhone")

        // 저장
        if (regionId != null && officeId != null) {
            prefs.saveOfficeInfo(officeId, regionId)
            android.util.Log.d("MainActivity", "✅ DeepLink - 사무실 정보 저장 완료")
        }

        if (officePhone != null && bankName != null && accountNumber != null && accountHolder != null) {
            prefs.saveOfficeContactInfo(officePhone, bankName, accountNumber, accountHolder)
            android.util.Log.d("MainActivity", "✅ DeepLink - 연락처 정보 저장 완료")
        }
    }
}
```

### 3. 랜딩 페이지 - 최신 코드 빌드 및 배포
**파일:** `customer_app/landing/app/page.tsx`

**기능:**
- FingerprintJS로 디바이스 정보 수집
- Firebase attributions 컬렉션에 저장
- 1초 후 자동으로 다운로드 페이지로 리다이렉트

**빌드 및 배포:**
```bash
cd customer_app/landing
npm run build
firebase deploy --only hosting
```

**배포 확인:**
- URL: https://calldetector-5d61e.web.app
- Build ID: -ZlKlvUCi1-6DiCfIjMcm
- 배포 시각: 2025-10-01 08:51

### 4. 고객앱 APK 재빌드 및 배포
**빌드:**
```bash
cd customer_app/app
./gradlew clean assembleDebug
```

**APK 위치:**
- 원본: `customer_app/app/app/build/outputs/apk/debug/app-debug.apk`
- 배포: `releases/customer_app.apk`

**Firebase 배포:**
```bash
firebase deploy --only hosting
```

### 5. 콜매니저 - QR 코드 클릭 시 확대 기능
**파일:** `call_manager/app/src/main/java/com/designated/callmanager/ui/attribution/AttributionManagementScreen.kt`

**추가된 기능:**
- QR 코드 이미지에 `.clickable()` modifier 추가
- `QRCodeEnlargedDialog` 컴포저블 추가
- 클릭 시 전체 화면에 가까운 크기로 확대 표시

## 테스트 결과

### ✅ 성공한 테스트
1. **핑거프린트 수집:** Firebase에 정상적으로 저장됨
   ```
   VisitorId: e3109a5172dacd0af5d4fef995a4dcdb
   Source: landing
   ```

2. **딥링크 파싱:** adb로 직접 테스트 시 완벽하게 작동
   ```
   ✅ DeepLink - 사무실 정보 저장 완료: regionId=Hongchon, officeId=qwfdeSOL8Vz4lXEEP4TD
   ✅ DeepLink - 연락처 정보 저장 완료: phone=01036702011, bank=NH농협은행
   ```

3. **QR 코드 확대:** 콜매니저에서 QR 클릭 시 다이얼로그로 확대 표시

### ❌ 해결 필요한 문제
1. **다운로드 페이지 "앱 열기" 버튼 미작동**
   - 버튼 클릭 시 딥링크가 앱에 전달되지 않음
   - 일반 앱 실행으로 처리됨 (Intent Action: MAIN, Intent Data: null)

## 확인해야 할 것

### 1. 다운로드 페이지 딥링크 실행
**파일:** `customer_app/landing/app/download/page.tsx:40-50`

현재 코드:
```typescript
const handleOpenApp = () => {
    // 딥링크 실행
    window.location.href = deepLinkUrl

    // 3초 후에도 앱이 안 열렸으면 APK 다운로드 안내
    setTimeout(() => {
      if (confirm('앱이 설치되어 있지 않습니다. APK를 다운로드하시겠습니까?')) {
        handleDownloadAPK()
      }
    }, 3000)
}
```

**문제:** `window.location.href`로 딥링크 실행이 제대로 안 될 수 있음

**확인 필요:**
- 브라우저에서 커스텀 scheme 실행 방식
- Android Chrome의 딥링크 처리 방식

### 2. SharedPreferences 데이터 확인
현재 `office1` 데이터가 여전히 남아있는 이유 확인 필요:
```
기존 저장된 OfficeId: office1
기존 저장된 RegionId: seoul
```

**확인 명령:**
```bash
adb shell pm clear com.designated.customer  # 앱 데이터 완전 삭제
```

### 3. 다운로드 페이지 Debug Info 확인
다운로드 페이지 하단에 표시되는 Debug Info의 딥링크 URL 확인:
```
Debug Info:
designatedcustomer://open?r=Hongchon&o=qwfdeSOL8Vz4lXEEP4TD&phone=01036702011&bank=NH%EB%86%8D%ED%98%91%EC%9D%80%ED%96%89&account=3333149014688&holder=ggg
```

## 앞으로 해야 할 것

### 1. 다운로드 페이지 "앱 열기" 버튼 수정 (최우선)
**목표:** 버튼 클릭 시 딥링크가 제대로 전달되도록 수정

**옵션 1: iframe 사용**
```typescript
const handleOpenApp = () => {
    const iframe = document.createElement('iframe')
    iframe.style.display = 'none'
    iframe.src = deepLinkUrl
    document.body.appendChild(iframe)

    setTimeout(() => {
        document.body.removeChild(iframe)
    }, 1000)
}
```

**옵션 2: Intent URL 사용 (Android 전용)**
```typescript
const handleOpenApp = () => {
    const intentUrl = `intent://open?r=${regionId}&o=${officeId}#Intent;scheme=designatedcustomer;package=com.designated.customer;end`
    window.location.href = intentUrl
}
```

**옵션 3: Universal Link 방식**
- Firebase Dynamic Links 또는 App Links 설정
- https:// URL로 변경하여 앱 실행

### 2. 앱 실행 플로우 개선
**현재 플로우:**
```
QR 스캔 → 랜딩 페이지 → 다운로드 페이지 → "앱 열기" 버튼 → 앱 실행
```

**개선 옵션:**
1. 랜딩 페이지에서 바로 딥링크 실행 시도
2. 앱이 설치되어 있으면 바로 실행
3. 설치되어 있지 않으면 다운로드 페이지로 이동

### 3. 에러 핸들링 추가
- 딥링크 실행 실패 시 사용자 안내
- APK 다운로드 실패 시 재시도 옵션
- Firebase 저장 실패 시 로그

### 4. 사용자 경험 개선
- 로딩 인디케이터 추가
- 진행 상황 표시
- 명확한 안내 메시지

### 5. 테스트 자동화
- QR 스캔부터 앱 실행까지 전체 플로우 테스트
- 여러 기기/브라우저에서 테스트
- 딥링크 파라미터 검증

## 테스트 방법

### 전체 플로우 테스트
1. **앱 완전 삭제:**
   ```bash
   adb uninstall com.designated.customer
   ```

2. **새 APK 설치:**
   ```bash
   adb install customer_app/app/app/build/outputs/apk/debug/app-debug.apk
   ```

3. **앱 데이터 삭제:**
   ```bash
   adb shell pm clear com.designated.customer
   ```

4. **Logcat 모니터링:**
   ```bash
   adb logcat MainActivity:D *:S
   ```

5. **QR 코드 스캔:**
   - 콜매니저에서 "ggg" 사무실 QR 생성
   - 시크릿 모드에서 스캔

6. **로그 확인:**
   ```
   ✅ DeepLink - 사무실 정보 저장 완료: regionId=Hongchon, officeId=qwfdeSOL8Vz4lXEEP4TD
   ✅ DeepLink - 연락처 정보 저장 완료: phone=01036702011, bank=NH농협은행
   ```

### adb로 직접 딥링크 테스트
```bash
adb shell am start -a android.intent.action.VIEW -d "designatedcustomer://open?r=Hongchon&o=qwfdeSOL8Vz4lXEEP4TD&phone=01036702011&bank=NH%EB%86%8D%ED%98%91%EC%9D%80%ED%96%89&account=3333149014688&holder=ggg"
```

## 주요 파일 위치

### Android (고객앱)
- `customer_app/app/app/src/main/AndroidManifest.xml`
- `customer_app/app/app/src/main/java/com/designated/customer/MainActivity.kt`
- `customer_app/app/app/src/main/java/com/designated/customer/util/PreferencesManager.kt`

### Next.js (랜딩페이지)
- `customer_app/landing/app/page.tsx` (랜딩)
- `customer_app/landing/app/download/page.tsx` (다운로드)
- `customer_app/landing/next.config.js`

### Firebase
- `firebase.json` (호스팅 설정)
- `releases/` (배포 폴더)
- `releases/customer_app.apk` (최신 APK)

### 콜매니저
- `call_manager/app/src/main/java/com/designated/callmanager/ui/attribution/AttributionManagementScreen.kt`

## Firebase 데이터 확인

### attributions 컬렉션 확인
```javascript
// functions 폴더에서 실행
node -e "
const admin = require('firebase-admin');
const serviceAccount = require('./service-account-key.json');
admin.initializeApp({ credential: admin.credential.cert(serviceAccount) });
const db = admin.firestore();

(async () => {
  const snapshot = await db
    .collection('regions')
    .doc('Hongchon')
    .collection('offices')
    .doc('qwfdeSOL8Vz4lXEEP4TD')
    .collection('attributions')
    .orderBy('createdAt', 'desc')
    .limit(5)
    .get();

  snapshot.forEach(doc => {
    const data = doc.data();
    console.log('VisitorId:', data.visitorId);
    console.log('Source:', data.source);
    console.log('CreatedAt:', data.createdAt?.toDate());
    console.log('---');
  });
  process.exit(0);
})();
"
```

## 알려진 이슈

1. **office1 데이터 잔존:** `pm clear` 후에도 office1 데이터가 남아있음
   - 원인: 앱이 실행 중일 때 메모리에 남아있을 수 있음
   - 해결: 앱 강제 종료 후 데이터 삭제

2. **다운로드 페이지 딥링크 미작동:** "앱 열기" 버튼 클릭 시 딥링크가 전달되지 않음
   - 원인: `window.location.href`가 커스텀 scheme을 제대로 처리하지 못함
   - 해결: iframe 또는 Intent URL 방식으로 변경 필요

3. **브라우저 캐시:** 시크릿 모드가 아닌 경우 이전 버전 페이지가 로드됨
   - 해결: 전체 기간 캐시 삭제 또는 시크릿 모드 사용

## 참고사항

### 딥링크 URL 형식
```
designatedcustomer://open?r={regionId}&o={officeId}&phone={phone}&bank={bank}&account={account}&holder={holder}
```

### QR 코드 URL 형식
```
https://calldetector-5d61e.web.app/?r={regionId}&o={officeId}&phone={phone}&bank={bank}&account={account}&holder={holder}
```

### 딥링크 작동 확인 로그
```
========== handleDeepLink 시작 ==========
Intent Action: android.intent.action.VIEW
Intent Data: designatedcustomer://open?r=...
추출된 파라미터:
  - regionId: Hongchon
  - officeId: qwfdeSOL8Vz4lXEEP4TD
  - phone: 01036702011
✅ DeepLink - 사무실 정보 저장 완료
✅ DeepLink - 연락처 정보 저장 완료
```

---

**작성일:** 2025-10-01
**상태:** 딥링크 구현 완료, 다운로드 페이지 버튼 수정 필요
**다음 작업:** download/page.tsx의 handleOpenApp() 함수 수정
