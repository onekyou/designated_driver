# 고객앱 사무실 연락처 정보 통합 작업내역

## 작업 일자
2025-01-XX (컨텍스트 계속)

## 작업 목표
QR 코드 스캔 → 회원가입 → 로그인 후, 고객앱에서 사무실 연락처 정보(전화번호, 계좌번호)를 사용할 수 있도록 통합 구현

---

## 완료된 작업

### 1. CustomerInfo 모델 확장 ✅
**파일**: `customer_app/app/app/src/main/java/com/designated/customer/data/model/CustomerInfo.kt`

**추가된 필드**:
```kotlin
val officePhone: String = "",
val bankName: String = "",
val accountNumber: String = "",
val accountHolder: String = ""
```

**목적**: 고객 정보에 사무실 연락처 정보를 포함하여 저장

---

### 2. PreferencesManager 확장 ✅
**파일**: `customer_app/app/app/src/main/java/com/designated/customer/util/PreferencesManager.kt`

**추가된 상수**:
```kotlin
private const val KEY_OFFICE_PHONE = "office_phone"
private const val KEY_BANK_NAME = "bank_name"
private const val KEY_ACCOUNT_NUMBER = "account_number"
private const val KEY_ACCOUNT_HOLDER = "account_holder"
```

**추가된 메서드**:
```kotlin
// 저장
fun saveOfficeContactInfo(
    officePhone: String,
    bankName: String,
    accountNumber: String,
    accountHolder: String
)

// 조회
fun getOfficePhone(): String?
fun getBankName(): String?
fun getAccountNumber(): String?
fun getAccountHolder(): String?
```

**목적**: QR 코드에서 받은 사무실 연락처 정보를 로컬에 저장/조회

---

### 3. MainActivity.handleDeepLink() 수정 ✅
**파일**: `customer_app/app/app/src/main/java/com/designated/customer/MainActivity.kt`

**변경 내용**:
```kotlin
private fun handleDeepLink(intent: Intent) {
    val data: Uri? = intent.data
    if (data != null) {
        // Query 파라미터에서 정보 추출
        val regionId = data.getQueryParameter("r")
        val officeId = data.getQueryParameter("o")
        val officePhone = data.getQueryParameter("phone")
        val bankName = data.getQueryParameter("bank")
        val accountNumber = data.getQueryParameter("account")
        val accountHolder = data.getQueryParameter("holder")

        // SharedPreferences에 저장
        val prefs = PreferencesManager(this)

        if (regionId != null && officeId != null) {
            prefs.saveOfficeInfo(officeId, regionId)
            android.util.Log.d("MainActivity", "DeepLink - 사무실 정보 저장: regionId=$regionId, officeId=$officeId")
        }

        if (officePhone != null && bankName != null && accountNumber != null && accountHolder != null) {
            prefs.saveOfficeContactInfo(officePhone, bankName, accountNumber, accountHolder)
            android.util.Log.d("MainActivity", "DeepLink - 연락처 정보 저장: phone=$officePhone, bank=$bankName")
        }
    }
}
```

**QR 코드 URL 형식** (콜매니저에서 생성):
```
https://calldetector-5d61e.web.app/?r={regionId}&o={officeId}&phone={전화번호}&bank={은행명}&account={계좌번호}&holder={예금주}
```

**목적**: QR 스캔 시 딥링크로 전달된 사무실 연락처 정보를 SharedPreferences에 저장

---

### 4. MainActivity.onAuthSuccess 수정 ✅
**파일**: `customer_app/app/app/src/main/java/com/designated/customer/MainActivity.kt`

**변경 내용**:
```kotlin
PhoneAuthScreen(
    modifier = Modifier.padding(paddingValues),
    onAuthSuccess = { phoneNumber ->
        // 전화번호 저장
        currentPhoneNumber = phoneNumber
        isPhoneVerified = true
        preferencesManager.savePhoneNumber(phoneNumber)

        // CustomerInfo 생성 및 Firebase 저장
        coroutineScope.launch {
            try {
                val regionId = currentRegionId
                val officeId = currentOfficeId
                val officePhone = preferencesManager.getOfficePhone()
                val bankName = preferencesManager.getBankName()
                val accountNumber = preferencesManager.getAccountNumber()
                val accountHolder = preferencesManager.getAccountHolder()

                if (regionId != null && officeId != null &&
                    officePhone != null && bankName != null &&
                    accountNumber != null && accountHolder != null) {

                    val firestore = com.google.firebase.firestore.FirebaseFirestore.getInstance()

                    // CustomerInfo 생성
                    val newCustomerInfo = com.designated.customer.data.model.CustomerInfo(
                        id = phoneNumber,
                        phoneNumber = phoneNumber,
                        name = "",
                        grade = "bronze",
                        points = 0,
                        totalRides = 0,
                        totalSpent = 0L,
                        linkedOfficeId = officeId,
                        primaryOfficeId = officeId,
                        attributionScore = null,
                        attributionSource = "qr_code",
                        registeredAt = com.google.firebase.Timestamp.now(),
                        lastRideAt = null,
                        officePhone = officePhone,
                        bankName = bankName,
                        accountNumber = accountNumber,
                        accountHolder = accountHolder
                    )

                    // Firebase에 저장
                    firestore
                        .collection("regions").document(regionId)
                        .collection("offices").document(officeId)
                        .collection("customers").document(phoneNumber)
                        .set(newCustomerInfo.toMap())
                        .await()

                    android.util.Log.d("MainActivity", "회원가입 - CustomerInfo 생성 및 저장 완료")

                    // 메모리에도 저장
                    customerInfo = newCustomerInfo
                }
            } catch (e: Exception) {
                android.util.Log.e("MainActivity", "CustomerInfo 생성 실패", e)
            }
        }
    }
)
```

**Firebase 저장 경로**:
```
regions/{regionId}/offices/{officeId}/customers/{phoneNumber}
```

**목적**: 전화번호 인증 완료 시 자동으로 CustomerInfo를 생성하여 Firebase에 저장

---

### 5. MainActivity LaunchedEffect 수정 (기존) ✅
**파일**: `customer_app/app/app/src/main/java/com/designated/customer/MainActivity.kt`

**기존 로직**: 로그인 후 CustomerInfo 로드
```kotlin
LaunchedEffect(isPhoneVerified, currentPhoneNumber, currentRegionId, currentOfficeId) {
    if (isPhoneVerified && currentPhoneNumber != null && currentRegionId != null && currentOfficeId != null) {
        try {
            val firestore = com.google.firebase.firestore.FirebaseFirestore.getInstance()
            val doc = firestore
                .collection("regions").document(currentRegionId!!)
                .collection("offices").document(currentOfficeId!!)
                .collection("customers").document(currentPhoneNumber!!)
                .get()
                .await()

            if (doc.exists()) {
                customerInfo = com.designated.customer.data.model.CustomerInfo.fromMap(doc.data ?: emptyMap())
                android.util.Log.d("MainActivity", "CustomerInfo 로드 성공: ${customerInfo?.officePhone}")
            }
        } catch (e: Exception) {
            android.util.Log.e("MainActivity", "CustomerInfo 로드 실패", e)
        }
    }
}
```

**목적**: 이미 회원가입한 사용자가 재로그인 시 CustomerInfo 자동 로드

---

### 6. MainViewModel 수정 (기존) ✅
**파일**: `customer_app/app/app/src/main/java/com/designated/customer/ui/main/MainViewModel.kt`

**추가된 필드**:
```kotlin
private val customerInfo: com.designated.customer.data.model.CustomerInfo?

val officePhone: String get() = customerInfo?.officePhone ?: ""
val bankName: String get() = customerInfo?.bankName ?: ""
val accountNumber: String get() = customerInfo?.accountNumber ?: ""
val accountHolder: String get() = customerInfo?.accountHolder ?: ""
```

**목적**: MainViewModel에서 사무실 연락처 정보에 접근 가능하도록 getter 제공

---

## 전체 데이터 플로우

### 회원가입 플로우
```
1. QR 코드 스캔
   ↓
2. 랜딩페이지 로드
   ↓
3. 앱 다운로드 링크 클릭
   ↓
4. 앱 설치 및 딥링크로 실행
   ↓ (URL: https://xxx/?r=seoul&o=office1&phone=010-1234-5678&bank=국민은행&account=123-456-789&holder=홍길동)
   ↓
5. handleDeepLink() 실행
   - regionId, officeId, officePhone, bankName, accountNumber, accountHolder
   - 모두 SharedPreferences에 저장
   ↓
6. 전화번호 인증 화면
   ↓
7. 인증 완료 (onAuthSuccess)
   - SharedPreferences에서 사무실 정보 조회
   - CustomerInfo 생성 (사무실 연락처 포함)
   - Firebase에 저장: regions/{regionId}/offices/{officeId}/customers/{phoneNumber}
   - 메모리(customerInfo 변수)에도 저장
   ↓
8. 메인 화면 진입
   - MainNavigation에 customerInfo 전달
   - MainViewModel에서 사무실 연락처 사용 가능
```

### 재로그인 플로우
```
1. 앱 실행
   ↓
2. SharedPreferences에서 사용자 정보 로드
   - isPhoneVerified = true
   - currentPhoneNumber, currentRegionId, currentOfficeId 존재
   ↓
3. LaunchedEffect 실행
   - Firebase에서 CustomerInfo 로드
   - regions/{regionId}/offices/{officeId}/customers/{phoneNumber}
   ↓
4. 메인 화면 진입
   - 사무실 연락처 정보 사용 가능
```

---

## 빌드 상태
✅ **BUILD SUCCESSFUL**

---

## 남은 작업 (다음 단계)

### 단계 1: 테스트 (최우선)
- [ ] QR 코드 생성 테스트 (콜매니저)
- [ ] QR 스캔 → 회원가입 → 로그인 플로우 테스트
- [ ] Firebase에 CustomerInfo 정상 저장 확인
- [ ] 재로그인 시 CustomerInfo 정상 로드 확인
- [ ] 로그 확인:
  - `DeepLink - 사무실 정보 저장`
  - `DeepLink - 연락처 정보 저장`
  - `회원가입 - CustomerInfo 생성 및 저장 완료`
  - `CustomerInfo 로드 성공`

### 단계 2: MainScreen UI 전면 재구성
- [ ] 만보기 영역 Spacer 예약
- [ ] 상단 헤더 간소화 - 사무실명과 포인트 한 줄 배치
- [ ] **중앙 큰 원형 전화 호출 버튼 추가** (tel: Intent로 사무실 전화)
  - `viewModel.officePhone` 사용
- [ ] 위치정보 카드 UI 유지 (출발지/목적지)
- [ ] 하단: 앱 호출 버튼 (기존 대리운전 호출 버튼)

### 단계 3: 콜 상태 관리 UI
- [ ] 앱 호출 버튼 클릭 시 '호출중...' 상태 표시
- [ ] 기사 배정 시 기사 정보 카드 (이름/연락처)
- [ ] IN_PROGRESS 상태 감지 로직 (Firebase Listener)

### 단계 4: 결제 시스템 구현
- [ ] IN_PROGRESS 감지 시 결제 카드 UI 표시
- [ ] 현금/이체/포인트 3가지 버튼
- [ ] 현금 결제 - '운행 종료' 팝업 표시
- [ ] **이체 결제 - 계좌번호 표시 및 클립보드 복사**
  - `viewModel.bankName`, `viewModel.accountNumber`, `viewModel.accountHolder` 사용
- [ ] 포인트 결제 UI - 잔여 포인트 표시, 전액/부분 사용 선택
- [ ] 포인트 사용 선택 시 PointService.usePoints() 호출
- [ ] 결제 완료 시 Firebase 업데이트 (paymentMethod, pointsUsed, cashAmount)

### 단계 5: 포인트 적립 (Cloud Functions)
- [ ] Cloud Functions: AWAITING_SETTLEMENT→COMPLETED 감지
- [ ] 현금 결제 부분만 포인트 적립 (등급별 비율)

---

## 주요 참고사항

### QR 코드 생성 위치
**콜매니저**: `call_manager/app/src/main/java/com/designated/callmanager/ui/attribution/AttributionManagementViewModel.kt`

```kotlin
val landingPageUrl = "https://calldetector-5d61e.web.app/?r=$regionId&o=$officeId" +
        "&phone=${android.net.Uri.encode(phone)}" +
        "&bank=${android.net.Uri.encode(bank)}" +
        "&account=${android.net.Uri.encode(account)}" +
        "&holder=${android.net.Uri.encode(holder)}"
```

### Firebase 컬렉션 구조
```
regions/
  └─ {regionId}/
      └─ offices/
          └─ {officeId}/
              └─ customers/
                  └─ {phoneNumber}/
                      ├─ id: String
                      ├─ phoneNumber: String
                      ├─ name: String
                      ├─ grade: String (bronze/silver/gold/vip)
                      ├─ points: Int
                      ├─ totalRides: Int
                      ├─ totalSpent: Long
                      ├─ linkedOfficeId: String
                      ├─ primaryOfficeId: String
                      ├─ attributionScore: Int?
                      ├─ attributionSource: String?
                      ├─ registeredAt: Timestamp
                      ├─ lastRideAt: Timestamp?
                      ├─ officePhone: String ← 새로 추가
                      ├─ bankName: String ← 새로 추가
                      ├─ accountNumber: String ← 새로 추가
                      └─ accountHolder: String ← 새로 추가
```

### 사무실 연락처 사용 예시
```kotlin
// MainViewModel에서
val officePhone: String get() = customerInfo?.officePhone ?: ""
val bankName: String get() = customerInfo?.bankName ?: ""
val accountNumber: String get() = customerInfo?.accountNumber ?: ""
val accountHolder: String get() = customerInfo?.accountHolder ?: ""

// UI에서 전화 걸기
val intent = Intent(Intent.ACTION_DIAL).apply {
    data = Uri.parse("tel:${viewModel.officePhone}")
}
context.startActivity(intent)

// UI에서 계좌번호 복사
val clipboardManager = context.getSystemService(Context.CLIPBOARD_SERVICE) as ClipboardManager
val clip = ClipData.newPlainText("계좌번호", "${viewModel.bankName} ${viewModel.accountNumber} (${viewModel.accountHolder})")
clipboardManager.setPrimaryClip(clip)
Toast.makeText(context, "계좌번호가 복사되었습니다", Toast.LENGTH_SHORT).show()
```

---

## 알려진 이슈
- 없음 (현재까지 빌드 성공, 테스트 필요)

---

## 관련 파일 목록
1. `customer_app/app/app/src/main/java/com/designated/customer/data/model/CustomerInfo.kt`
2. `customer_app/app/app/src/main/java/com/designated/customer/util/PreferencesManager.kt`
3. `customer_app/app/app/src/main/java/com/designated/customer/MainActivity.kt`
4. `customer_app/app/app/src/main/java/com/designated/customer/ui/main/MainViewModel.kt`
5. `customer_app/app/app/src/main/java/com/designated/customer/ui/main/HomeScreen.kt`
6. `customer_app/app/app/src/main/java/com/designated/customer/ui/main/MainScreen.kt`
7. `customer_app/app/app/src/main/java/com/designated/customer/ui/navigation/MainNavigation.kt`

---

## 이전 대화 컨텍스트
- QR 없이는 앱 다운로드 자체가 불가능 (OfficeSelectionScreen은 사실상 데드 코드)
- 콜매니저에서 QR 생성 시 사무실 연락처 정보를 URL 파라미터에 포함
- Attribution 시스템은 QR 기반으로 단순화 (FingerprintJS 불필요)
- 2가지 호출 방식: 전화 호출(tel: Intent) + 앱 호출(Firebase + FCM)

---

## 작성자 메모
이 작업은 고객앱의 핵심 기능인 사무실 연락처 정보 통합을 완료한 것입니다.
다음 챗에서는 테스트를 먼저 진행한 후, MainScreen UI 재구성 작업을 시작하면 됩니다.
