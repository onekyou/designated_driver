# 자동공유콜 알림 시스템 구현 진행상황

## 📋 현재 상황 요약

### 문제 정의
- **현상**: 업무마감시 자동공유콜이 shared_calls에는 등록되지만, FCM 알림과 팝업이 생성되지 않음
- **영향**: 다른 사무실에서 퇴근 후 공유콜을 확인할 수 없어 콜 유실 발생

### 원인 분석 완료 ✅
**핵심 문제**: CallDetectorService의 자동공유콜 생성 시 `targetRegionId` 필드 누락
- Cloud Functions `onSharedCallCreated`가 `sharedCallData.targetRegionId`를 찾지 못함
- FCM 알림 대상 관리자 쿼리 실패로 알림 전송되지 않음

## 🔧 수정 작업 진행상황

### 완료된 작업 ✅
1. **문제 원인 정확히 파악**: targetRegionId 필드 누락 확인
2. **해결 방안 수립**: 자동공유콜 생성 시 targetRegionId 추가
3. **부분 수정 완료**: 3개 함수 중 일부 수정

### 현재 진행중인 작업 🔄
**CallDetectorService.kt 컴파일 오류 해결**
- **위치**: `call_manager/app/src/main/java/com/designated/callmanager/service/CallDetectorService.kt:713:13`
- **오류**: `Type mismatch: inferred type is Nothing? but Any was expected`
- **원인**: HashMap<String, Any>에 null 값 할당 시 타입 불일치

## 📝 수정해야 할 파일들

### CallDetectorService.kt 수정 내역
다음 3개 함수에 `"targetRegionId" to regionId` 추가 필요:

#### 1. createSharedCall() - 업무 마감 후 콜
```kotlin
// 위치: 라인 693-725 근처
private fun createSharedCall(regionId: String, officeId: String, ...)
```

#### 2. createSharedCallFromMissed() - 부재중 전화 ✅ 완료
```kotlin
// 위치: 라인 415-447 근처  
private fun createSharedCallFromMissed(regionId: String, officeId: String, ...)
```

#### 3. createSharedCallFromRinging() - 마감 후 빠른 응답 ✅ 완료
```kotlin
// 위치: 라인 893-925 근처
private fun createSharedCallFromRinging(regionId: String, officeId: String, ...)
```

## ⚠️ 현재 블로킹 이슈

### 컴파일 오류 상세
- **파일**: CallDetectorService.kt
- **라인**: 713:13
- **오류 메시지**: `Type mismatch: inferred type is Nothing? but Any was expected`
- **추정 원인**: null 값을 HashMap<String, Any>에 직접 할당

### 시도한 해결책
1. ❌ null 필드 제거 시도 - 여전히 오류 지속
2. 추가 조사 필요

## 🎯 다음 단계 작업 계획

### 우선순위 1: 컴파일 오류 해결
1. **CallDetectorService.kt 오류 정확한 위치 파악**
   - 713번 라인 주변 코드 상세 분석
   - HashMap 타입 선언 확인

2. **타입 안전한 방식으로 수정**
   ```kotlin
   // 옵션 1: 타입 명시적 선언
   val sharedCallData = hashMapOf<String, Any?>()
   
   // 옵션 2: 조건부 추가
   if (departure != null) sharedCallData["departure"] = departure
   
   // 옵션 3: 기본값 사용
   "departure" to (departure ?: "")
   ```

### 우선순위 2: 완전한 수정 완료
1. **남은 수정 작업**
   - createSharedCall() 함수 targetRegionId 추가 확인
   - 모든 자동공유콜 생성 경로 점검

2. **기능 테스트**
   - 업무마감 설정 후 퇴근 후 콜 테스트
   - FCM 알림 정상 수신 확인
   - 공유콜 수락 팝업 생성 확인

### 우선순위 3: 추가 보완 기능 (선택)
1. **포그라운드 리스너 구현**
   - DashboardViewModel에 shared_calls 실시간 감지
   - FCM 실패 시 백업 알림 메커니즘

## 📊 예상 결과

### 수정 완료 후 동작 흐름
```
1. 퇴근 후 전화 수신 📞
2. CallDetectorService가 shared_calls에 데이터 저장
   - sourceRegionId: "지역ID"  
   - sourceOfficeId: "사무실ID"
   - targetRegionId: "지역ID" ✅ (새로 추가)
3. Cloud Functions onSharedCallCreated 트리거 ✅
4. targetRegionId로 관리자 FCM 토큰 조회 성공 ✅
5. 같은 지역의 다른 사무실들에 FCM 알림 전송 📱
6. 관리자가 알림 클릭
7. 공유콜 수락 팝업 표시 ✅
8. 정상적인 공유콜 처리 완료
```

## 🔍 관련 파일 목록

### 주요 수정 파일
- `call_manager/app/src/main/java/com/designated/callmanager/service/CallDetectorService.kt`

### 연관 파일들 (참고용)
- `functions/src/index.ts` - onSharedCallCreated Cloud Function
- `call_manager/app/src/main/java/com/designated/callmanager/ui/dashboard/DashboardViewModel.kt`
- `call_manager/app/src/main/java/com/designated/callmanager/MainActivity.kt`

## 💡 중요 참고사항

### 안전성 확인
- ✅ 기존 수동 공유콜 로직에 영향 없음
- ✅ 필드 추가는 NoSQL에서 안전한 변경
- ✅ 기존 데이터와 완벽 호환
- ✅ 보안 규칙에 영향 없음

### 테스트 시나리오
1. **기존 기능 확인**: 수동 공유콜 정상 동작 확인
2. **새 기능 테스트**: 자동공유콜 FCM 알림 수신 확인
3. **통합 테스트**: 포그라운드/백그라운드 상황별 팝업 생성 확인

---
**작성일**: 2025-09-06  
**작성자**: Claude Code Assistant  
**다음 작업 담당자**: 컴파일 오류 해결 후 기능 테스트 진행