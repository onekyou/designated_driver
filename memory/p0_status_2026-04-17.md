---
name: p0_status_2026-04-17
description: REVIEW_FINDINGS.md의 P0 9건 재검증 결과 (2026-04-17 세션). 어느 건이 완료/미패치/false positive인지 추적
type: project
---

# P0 9건 재검증 결과 (2026-04-17)

**배경**: 4/16 `flutter/REVIEW_FINDINGS.md` 작성 후 Flutter 코딩 진입 전 P0 9건 패치 필요성 제기. 본 세션에서 실제 상태 재검증.

**Why**: 메모리상 "P0 9건 미패치"로만 기록되어 실제 진행 상태 혼선 발생. 세션 간 일관성 위해 스냅샷 필요.

**How to apply**: Phase 6 Flutter Dart 재작성 진입 전 본 상태 기준으로 남은 6건 패치 우선.

## 상태 스냅샷

| # | 항목 | 상태 | 위치/근거 |
|---|------|------|---------|
| A.1 | Platform.isIOS import 누락 | ❌ 미패치 | `flutter/driver_app/MVP/NOTIFIERS.md:78-82` + `:917-922` imports에 `dart:io` 없음 |
| A.2 | StateNotifier ref 미주입 (가장 critical) | ❌ 미패치 | `final Ref ref` / `ref: this.ref` 검색 0건. DriverWorkflowNotifier + CarryOverNotifier 양쪽 미수정 |
| A.3 | _subscribeAuth 재진입 방지 부재 | ❌ 미패치 | `driver NOTIFIERS.md:115` 이전 구독 cancel 없이 덮어씀. 라인 1200-1283 CarryOverNotifier도 동일 |
| B.1 | AuthUiState 9필드 정의 | 🟡 부분 확인 | `customer MODELS.md:1031-1042` Freezed 정의. 4/9 확인 (isCodeSent, isAnonymouslySignedIn, anonymousUid, verificationId). 나머지 5 필드 추가 검증 대기 |
| B.2 | calculateEarnPoints 메서드 누락 | ✅ False positive | `customer MODELS.md:481` `int calculateEarnPoints(int fare) => grade.calculatePoints(fare);` 이미 존재. `const CustomerPoints._()` private 생성자 보강만 선택사항 |
| B.3 | grade.jsonValue → grade.json | ❌ 미패치 | `customer NOTIFIERS.md:257` `grade.jsonValue.toLowerCase()` 그대로 |
| C.1 | Dart let extension 오용 | ❌ 미패치 | `driver MODELS.md:1282-1283` `(kakao['y'] as String?)?.let((s) => double.tryParse(s))` 그대로 |
| D.1 | recordAcceptanceEvent driver 경로 | ✅ 패치 완료 | `flutter/SERVER_TASKS.md:301-394` (커밋 2026-04-17 세션). 보너스 P1-17 (`assignedTime` → `assignedTimestamp`) + P1-18 (backfill batch 재생성) |
| E.1 (a) | customerInfo 경로 오류 | ✅ 이미 정확 | `flutter/SERVER_TASKS.md:626` `/provinces/{p}/cities/{c}/offices/{o}/customerInfo/{phone}` 중첩 경로 |
| E.1 (b) | customerInfo 권한 취약점 | ❌ 미해결 | 현행 `request.auth != null`만으로 모든 인증 사용자가 모든 phone의 fcmToken 덮어쓰기 가능 (Anonymous 포함). phone-uid 매핑 설계 필요 — 별도 보안 의제 |

## 집계

- 완료 (커밋 반영): **2건** — D.1, E.1(a) (+ 보너스 P1-17, P1-18)
- 오늘 패치 예정: **5건** — A.1, A.2, A.3, B.3, C.1
- False positive: **1건** — B.2
- 별도 검증 필요 (Phase B 세션): **2건** — B.1 나머지 5필드, E.1(b) 보안 설계

## 참조

- 원본 발견: `flutter/REVIEW_FINDINGS.md` (c290d198, 2026-04-16)
- 본 재검증 세션: 2026-04-17
- 패치 계획: `C:\Users\kala1\.claude\plans\greedy-imagining-storm.md` 단계 4
