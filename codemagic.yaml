workflows:
  ios-driver-test-build:
    name: Driver iOS Test Build (Phase A - No Codesign)
    instance_type: mac_mini_m2
    max_build_duration: 90
    environment:
      flutter: 3.41.6
      xcode: latest
      cocoapods: default
    cache:
      cache_paths:
        - $HOME/Library/Caches/CocoaPods
        - $FLUTTER_ROOT/.pub-cache
    triggering:
      events: []
    scripts:
      - name: Flutter version check
        working_directory: driver_app_flutter
        script: flutter --version
      - name: Flutter pub get
        working_directory: driver_app_flutter
        script: flutter pub get
      - name: Flutter analyze (informational)
        working_directory: driver_app_flutter
        script: flutter analyze --no-fatal-infos || true
      - name: Flutter unit test (payload helper)
        working_directory: driver_app_flutter
        script: flutter test test/fcm_token_payload_test.dart
      - name: CocoaPods install
        working_directory: driver_app_flutter/ios
        script: pod install --repo-update
      - name: Boot iOS Simulator
        script: |
          set -e
          echo "--- Available iPhone simulators ---"
          xcrun simctl list devices available | grep -E "iPhone" || true
          DEVICE=$(xcrun simctl list devices available | grep -E "iPhone 1[6-9]|iPhone 15" | grep -v unavailable | head -n1 | sed -E 's/.*\(([A-F0-9-]{36})\).*/\1/')
          if [ -z "$DEVICE" ]; then
            DEVICE=$(xcrun simctl list devices available | grep -E "iPhone" | grep -v unavailable | head -n1 | sed -E 's/.*\(([A-F0-9-]{36})\).*/\1/')
          fi
          if [ -z "$DEVICE" ]; then
            echo "ERROR: No iPhone simulator available"
            xcrun simctl list devices available
            exit 1
          fi
          echo "Selected BOOT_ID=$DEVICE"
          echo "BOOT_ID=$DEVICE" >> $CM_ENV
          xcrun simctl boot "$DEVICE" 2>&1 || echo "(already booted or boot returned non-zero)"
          xcrun simctl bootstatus "$DEVICE" -b
          echo "Simulator boot complete."
      - name: Flutter integration_test on iOS Simulator
        working_directory: driver_app_flutter
        script: |
          flutter test integration_test/platform_field_test.dart \
            -d "$BOOT_ID"
      - name: Flutter build iOS (no codesign)
        working_directory: driver_app_flutter
        script: flutter build ios --release --no-codesign
    artifacts:
      - driver_app_flutter/build/ios/iphoneos/Runner.app
      - /tmp/xcodebuild_logs/*.log
