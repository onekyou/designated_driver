# 기사앱 Flutter 전환 작업 진행현황 (2025-12-30)

## 📋 프로젝트 개요

### 목표
기존 Android Kotlin 기사앱을 Flutter로 전환하여 iOS 출시 준비

### 앱 구분
| 구분 | 경로 | 언어/프레임워크 | 상태 | 용도 |
|------|------|----------------|------|------|
| **기존 기사앱** | `driver_app/` | Kotlin + Jetpack Compose | ✅ 완성 (Android 전용) | 비교 참조용 |
| **신규 기사앱** | `driver_app_flutter/` | Flutter + Riverpod | 🔄 개발 중 (iOS/Android) | 현재 작업 대상 |

---

## ✅ 완료된 작업 (Phase 1~3)

### Phase 1: Firebase 설정 및 기본 구성
**완료일:** 2025-12-30

#### 생성/수정 파일
```
driver_app_flutter/
├── lib/firebase_options.dart                    ✅ 생성 (Android/iOS Firebase 설정)
├── ios/Runner/Info.plist                        ✅ 수정 (권한 3개 추가)
├── pubspec.yaml                                 ✅ 수정 (패키지 18개 추가)
├── android/build.gradle                         ✅ 수정 (Gradle 설정)
└── android/app/build.gradle                     ✅ 수정 (compileSdk=34 하드코딩)
```

#### 주요 변경사항
- Firebase 프로젝트: `calldetector-5d61e` 통합
- iOS 권한 추가: 마이크, 음성인식, 사진 라이브러리
- 패키지 활성화:
  - `geolocator: ^10.1.0` (위치)
  - `geocoding: ^2.1.1` (주소 변환)
  - `speech_to_text: ^6.5.1` (음성 입력)
  - `flutter_local_notifications: ^16.3.0` (로컬 알림)
- Freezed 코드 생성: 883개 파일

---

### Phase 2: Presentation 기반 구축
**완료일:** 2025-12-30

#### 생성 파일 (8개)
```
lib/features/
├── call/presentation/providers/
│   ├── call_state.dart                         ✅ Freezed 상태 (initial/loading/loaded/error)
│   └── call_notifier.dart                      ✅ StateNotifier (콜 관리)
│
├── trip/presentation/providers/
│   ├── trip_state.dart                         ✅ Freezed 상태
│   └── trip_notifier.dart                      ✅ StateNotifier (운행 관리)
│
├── driver/presentation/providers/
│   ├── driver_state.dart                       ✅ Freezed 상태
│   └── driver_notifier.dart                    ✅ StateNotifier (기사 상태)
│
└── location/
    ├── domain/entities/position.dart           ✅ 위치 Entity
    └── presentation/providers/
        ├── location_state.dart                 ✅ Freezed 상태
        └── location_notifier.dart              ✅ StateNotifier (위치 추적)
```

#### 수정사항
- **중요 버그 수정:** Freezed 네이밍 충돌
  - `Call? call` → `Call? assignedCall` (call 메서드와 충돌)
  - `Trip? trip` → `Trip? currentTrip`

---

### Phase 3: FCM 서비스 + HomeScreen UI
**완료일:** 2025-12-30

#### 생성 파일 (2개)
```
lib/
├── services/fcm_service.dart                   ✅ 207줄 (FCM 통합 서비스)
└── screens/home_screen_new.dart                ✅ 756줄 (Riverpod 기반 완전 재구현)
```

#### FCM 서비스 (`fcm_service.dart`)
**기능:**
- FCM 토큰 가져오기 및 Firestore 저장
- 알림 권한 요청 (Android/iOS)
- Foreground/Background/Terminated 메시지 처리
- 로컬 알림 표시

**알림 타입:**
```dart
static const String notificationTypeCallAssigned = 'CALL_ASSIGNED';
static const String notificationTypeCallCancelled = 'CALL_CANCELLED';
static const String notificationTypeCallUpdated = 'CALL_UPDATED';
```

#### HomeScreen 구현 (`home_screen_new.dart`)
**3개 상태별 화면:**

##### 1. _WaitingScreen (대기 화면)
```
- Icon: access_time (노란색)
- 메시지: "새로운 콜을 기다리고 있습니다..."
- 버튼 1: "배차 확인" → fetchAssignedCall()
- 버튼 2: "고객 추천하기" → QR 화면 이동
- 배경색: Color(0xFF121212) 다크 테마
```

##### 2. _CallAssignedScreen (콜 배정 화면)
```
표시 정보:
✓ 고객명 (assignedCall.customerName)
✓ 전화번호 (assignedCall.phoneNumber)
✓ 출발지 (assignedCall.pickupLocation)
✓ 목적지 (assignedCall.destination)
✓ 예상 요금 (assignedCall.fare)
✓ 메모 (assignedCall.notes)

버튼:
- [거부] → rejectCall(callId, '기사가 거부')
- [수락하고 운행 시작] → acceptCall(callId)
```

##### 3. _OnTripScreen (운행 중 화면)
```
카드 3개:
1. 고객 정보 카드 (이름, 전화)
2. 경로 정보 카드 (출발지, 목적지)
3. 요금 정보 카드 (예상 요금)

버튼:
- [운행 완료] → 확인 다이얼로그 → completeCall(callId)
```

#### 기사 상태별 화면 전환
```dart
switch (driver.status) {
  case DriverStatus.online:
  case DriverStatus.waiting:
    return _WaitingScreen();        // 대기

  case DriverStatus.assigned:
  case DriverStatus.accepted:
    return _CallAssignedScreen();   // 콜 배정됨

  case DriverStatus.inProgress:
    return _OnTripScreen();         // 운행 중

  default:
    return _WaitingScreen();
}
```

---

## 🔧 수정된 에러 (13개)

### 1. Entity 필드명 불일치 (3개)
```diff
- assignedCall.pickupAddress    ❌
+ assignedCall.pickupLocation   ✅

- assignedCall.estimatedFare    ❌
+ assignedCall.fare             ✅
```

### 2. Enum 값 불일치 (1개)
```diff
- DriverStatus.onTrip           ❌
+ DriverStatus.inProgress       ✅
```

### 3. 존재하지 않는 필드 (1개)
```diff
- session.officeName            ❌ (UserSession에 없음)
+ '기사앱' (하드코딩)           ✅
```

### 4. UseCase 파라미터 누락 (3개)
```diff
- acceptCall()                  ❌
+ acceptCall(callId)            ✅

- rejectCall()                  ❌
+ rejectCall(callId, reason)    ✅

- completeCall()                ❌
+ completeCall(callId)          ✅
```

### 5. 기타 (5개)
- Gradle afterEvaluate 중복 블록 제거
- 사용하지 않는 import 제거 (trip_notifier)
- null-aware 경고 해결
- BuildContext async gap 경고

---

## 📊 코드 분석 결과

### Flutter Analyze
```
이전: 57개 이슈
현재: 44개 이슈 (23% 개선)

home_screen_new.dart: 0개 에러 ✅
```

### 남은 이슈 (44개)
```
- info (27개): 스타일 제안 (use_super_parameters, deprecated_member_use 등)
- warning (7개): 사용하지 않는 변수/import
- error (10개): Provider 초기화 시 driverId 파라미터 누락 (UI에서 세션 정보 전달 필요)
```

---

## 🔄 Android 앱 vs Flutter 앱 비교

### 아키텍처 비교

| 항목 | Android 앱 (driver_app) | Flutter 앱 (driver_app_flutter) |
|------|------------------------|--------------------------------|
| **언어** | Kotlin | Dart |
| **UI 프레임워크** | Jetpack Compose | Flutter Widgets |
| **상태 관리** | ViewModel + StateFlow | Riverpod + StateNotifier |
| **DI** | Hilt | GetIt |
| **비동기** | Coroutines + Flow | Future + Stream |
| **네비게이션** | Navigation Component | Navigator 2.0 |
| **불변 객체** | data class | Freezed |
| **플랫폼** | Android 전용 | iOS + Android |

### 파일 구조 비교

#### Android 앱 (Kotlin)
```
driver_app/app/src/main/java/com/designated/driver/
├── MainActivity.kt
├── ui/
│   ├── home/
│   │   ├── HomeScreen.kt
│   │   └── HomeViewModel.kt
│   ├── call/
│   │   └── CallAssignedScreen.kt
│   └── trip/
│       └── OnTripScreen.kt
├── data/
│   ├── repository/
│   └── model/
└── domain/
    ├── usecase/
    └── entity/
```

#### Flutter 앱 (Dart)
```
driver_app_flutter/lib/
├── main.dart
├── screens/
│   └── home_screen_new.dart          ← 3개 화면 모두 포함
├── features/
│   ├── call/
│   │   ├── domain/
│   │   │   ├── entities/call.dart
│   │   │   └── usecases/
│   │   ├── data/
│   │   │   └── repositories/
│   │   └── presentation/
│   │       └── providers/
│   │           ├── call_state.dart
│   │           └── call_notifier.dart
│   ├── trip/
│   ├── driver/
│   └── location/
└── services/
    └── fcm_service.dart
```

### 주요 차이점

#### 1. 화면 구성
**Android:**
- HomeScreen, CallAssignedScreen, OnTripScreen이 별도 파일
- Fragment 또는 Composable 함수로 분리
- Navigation Graph로 화면 전환

**Flutter:**
- home_screen_new.dart 1개 파일에 3개 화면 모두 포함
- private class로 구현 (_WaitingScreen, _CallAssignedScreen, _OnTripScreen)
- switch문으로 driver.status에 따라 화면 전환

#### 2. 상태 관리
**Android (Kotlin):**
```kotlin
class HomeViewModel : ViewModel() {
    private val _uiState = MutableStateFlow<HomeUiState>(HomeUiState.Loading)
    val uiState: StateFlow<HomeUiState> = _uiState.asStateFlow()

    fun fetchCall() {
        viewModelScope.launch {
            callRepository.getAssignedCall()
                .collect { result ->
                    _uiState.value = when(result) {
                        is Success -> HomeUiState.Loaded(result.data)
                        is Error -> HomeUiState.Error(result.message)
                    }
                }
        }
    }
}
```

**Flutter (Dart):**
```dart
class CallNotifier extends StateNotifier<CallState> {
  final GetAssignedCallUseCase _getAssignedCallUseCase;

  Future<void> fetchAssignedCall() async {
    state = const CallState.loading();

    final result = await _getAssignedCallUseCase(NoParams());

    result.fold(
      (failure) => state = CallState.error(failure.message),
      (call) => state = CallState.loaded(call),
    );
  }
}
```

#### 3. UI 작성
**Android (Compose):**
```kotlin
@Composable
fun WaitingScreen() {
    Column(
        modifier = Modifier.fillMaxSize(),
        horizontalAlignment = Alignment.CenterHorizontally
    ) {
        Icon(Icons.Default.AccessTime, tint = Color(0xFFFFB000))
        Text("새로운 콜을 기다리고 있습니다...")
        Button(onClick = { viewModel.fetchCall() }) {
            Text("배차 확인")
        }
    }
}
```

**Flutter:**
```dart
class _WaitingScreen extends ConsumerWidget {
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Container(
      color: const Color(0xFF121212),
      child: Column(
        children: [
          const Icon(Icons.access_time, color: Color(0xFFFFB000)),
          const Text('새로운 콜을 기다리고 있습니다...'),
          ElevatedButton(
            onPressed: () {
              ref.read(callNotifierProvider.notifier).fetchAssignedCall();
            },
            child: const Text('배차 확인'),
          ),
        ],
      ),
    );
  }
}
```

#### 4. FCM 구현
**Android:**
```kotlin
// MyFirebaseMessagingService.kt
class MyFirebaseMessagingService : FirebaseMessagingService() {
    override fun onMessageReceived(message: RemoteMessage) {
        when (message.data["type"]) {
            "CALL_ASSIGNED" -> showNotification()
        }
    }
}
```

**Flutter:**
```dart
// fcm_service.dart
class FcmService {
  void listenToForegroundMessages(Function(RemoteMessage) onMessage) {
    FirebaseMessaging.onMessage.listen((message) {
      _showLocalNotification(message);
      onMessage(message);
    });
  }
}
```

---

## 📝 다음 단계 (Phase 4~6)

### Phase 4: 운행 관리 시스템 구현
**우선순위: 높음**

#### 구현할 화면
1. **TripPreparationScreen (운행 준비 화면)**
   ```
   목적: 콜 수락 후 픽업 전까지
   기능:
   - 출발지까지 네비게이션 시작
   - "픽업 완료" 버튼
   - 고객 전화 걸기 버튼
   ```

2. **InProgressScreen (운행 중 화면) - 개선**
   ```
   현재: _OnTripScreen (스텁)
   추가 기능:
   - 실시간 위치 추적
   - 목적지까지 남은 거리/시간
   - 경로 이탈 감지
   - 중간 경유지 추가 (waypoints)
   ```

#### 필요한 파일
```
lib/
├── features/trip/
│   ├── domain/
│   │   ├── entities/
│   │   │   └── trip.dart                    ← 확인 필요
│   │   └── usecases/
│   │       ├── start_trip_usecase.dart
│   │       ├── update_location_usecase.dart
│   │       └── complete_trip_usecase.dart
│   └── presentation/
│       ├── screens/
│       │   ├── trip_preparation_screen.dart  ← 신규
│       │   └── in_progress_screen.dart       ← 신규
│       └── providers/
│           └── trip_notifier.dart            ← 이미 있음
└── services/
    ├── location_service.dart                 ← 신규 (geolocator 래핑)
    └── navigation_service.dart               ← 신규 (지도 앱 연동)
```

#### Android 앱 참조 파일
```
driver_app/app/src/main/java/com/designated/driver/
├── ui/trip/
│   ├── TripPreparationScreen.kt
│   ├── InProgressScreen.kt
│   └── TripViewModel.kt
└── service/
    └── LocationTrackingService.kt
```

---

### Phase 5: 정산 시스템 구현
**우선순위: 중간**

#### 구현할 화면
1. **SettlementPopup (정산 팝업)**
   ```
   목적: 운행 완료 직후
   입력:
   - 실제 요금 (음성 입력 가능)
   - 고객이 사용한 포인트
   - 메모
   버튼: [취소] [정산 완료]
   ```

2. **HistorySettlementScreen (운행내역)**
   ```
   목적: 과거 운행 조회
   표시:
   - 날짜별 운행 목록
   - 총 수입 통계
   - 필터 (날짜 범위, 상태)
   ```

#### 필요한 파일
```
lib/
├── features/settlement/
│   ├── domain/
│   │   ├── entities/settlement.dart          ← 신규
│   │   └── usecases/
│   │       ├── create_settlement_usecase.dart
│   │       └── get_settlement_history_usecase.dart
│   ├── data/
│   │   └── repositories/
│   │       └── settlement_repository_impl.dart
│   └── presentation/
│       ├── screens/
│       │   ├── settlement_popup.dart         ← 신규
│       │   └── history_settlement_screen.dart ← 신규
│       └── providers/
│           └── settlement_notifier.dart      ← 신규
└── services/
    └── speech_service.dart                   ← 신규 (speech_to_text)
```

---

### Phase 6: 부가 기능 구현
**우선순위: 낮음**

#### 구현할 화면/기능
1. **ReferralQRScreen (고객 추천 QR)**
   ```
   목적: 기사별 추천 QR 코드 생성
   표시:
   - QR 코드 (기사 ID 포함)
   - 저장/공유 버튼
   ```

2. **DriverRegistrationScreen (회원가입)**
   ```
   목적: 신규 기사 가입
   입력:
   - 이름, 전화번호, 이메일
   - 소속 지역/사무실 선택
   - 차량 정보
   ```

3. **음성 입력 통합**
   ```
   적용 화면:
   - SettlementPopup: 요금 음성 입력
   - 향후 주소 입력 시에도 사용 가능
   ```

---

## 🚀 빌드 및 실행 방법

### 방법 1: Android Studio (권장)
```
1. Android Studio 실행
2. File → Open → driver_app_flutter 폴더
3. 우측 하단 "Flutter SDK" 확인 (C:\src\flutter)
4. 상단 device 선택 (에뮬레이터/실제 기기)
5. Shift+F10 (Run) 또는 녹색 ▶ 버튼

빌드만:
- Build → Flutter → Build APK (Android)
- Build → Flutter → Build iOS (Mac에서만 가능)
```

### 방법 2: Flutter CLI (Windows 권한 이슈 있음)
```bash
cd C:\app_dev\designated_driver\driver_app_flutter

# 분석
flutter analyze

# 빌드
flutter build apk --debug    # Android
flutter build ios --debug    # iOS (Mac 필요)

# 실행
flutter run
```

### 방법 3: 배치 파일 (작성됨)
```
C:\app_dev\designated_driver\driver_app_flutter\build_debug.bat

더블클릭 또는 cmd에서 실행
```

---

## ⚠️ 주의사항

### 1. Git Bash 권한 문제
**현상:**
```
/c/src/flutter/bin/flutter.bat: Permission denied
```

**해결:**
- Android Studio 사용 권장
- 또는 cmd.exe 사용

### 2. 남은 컴파일 에러 (10개)
**위치:** Provider 초기화 시
```dart
// lib/features/call/presentation/providers/call_notifier.dart:31
final result = await _getAssignedCallUseCase(NoParams()); // ❌

// 필요한 것:
final result = await _getAssignedCallUseCase(
  GetAssignedCallParams(driverId: session.driverId)
); // ✅
```

**해결 시점:**
- Phase 4에서 실제 세션 정보를 Provider에 전달하면서 수정 예정

### 3. Firebase 프로젝트
**프로젝트 ID:** `calldetector-5d61e`

- Android: `com.designated.driverapp`
- iOS: `com.designated.driverapp` (iOS 번들 ID 확인 필요)

### 4. 의존성 충돌 가능성
**최초 설치 시 실행:**
```bash
cd driver_app_flutter
flutter pub get
flutter pub run build_runner build --delete-conflicting-outputs
```

---

## 📂 주요 파일 경로 정리

### 현재 작업 중인 Flutter 앱
```
C:\app_dev\designated_driver\driver_app_flutter\
```

### 참조용 Android 앱
```
C:\app_dev\designated_driver\driver_app\
```

### Firebase 설정
```
driver_app_flutter/
├── android/app/google-services.json
├── ios/Runner/GoogleService-Info.plist
└── lib/firebase_options.dart
```

### 핵심 파일
```
driver_app_flutter/lib/
├── main.dart                                    - 앱 진입점, FCM 백그라운드 핸들러
├── screens/home_screen_new.dart                 - 메인 화면 (756줄)
├── services/fcm_service.dart                    - FCM 서비스 (207줄)
└── features/
    ├── call/presentation/providers/
    │   ├── call_state.dart                      - 콜 상태 정의
    │   └── call_notifier.dart                   - 콜 비즈니스 로직
    ├── driver/presentation/providers/
    │   └── driver_notifier.dart                 - 기사 상태 관리
    └── auth/presentation/providers/
        └── auth_notifier.dart                   - 인증 상태 관리
```

---

## 🎯 즉시 할 일 (재부팅 후)

### 1. Android Studio에서 빌드 테스트
```
1. Android Studio 열기
2. driver_app_flutter 프로젝트 열기
3. Shift+F10 실행
4. 에러 메시지 확인 후 수정
```

### 2. Phase 4 시작 (위치 추적 + 운행 관리)
```
우선순위:
1. location_service.dart 구현 (geolocator 사용)
2. trip_preparation_screen.dart 구현
3. in_progress_screen.dart 개선
```

### 3. Android 앱 참조하며 로직 이식
```
참조 파일:
- driver_app/app/src/main/java/com/designated/driver/ui/trip/
- driver_app/app/src/main/java/com/designated/driver/service/LocationTrackingService.kt
```

---

## 📞 문의 사항

### 확인 필요
1. iOS Bundle ID가 `com.designated.driverapp`이 맞는지?
2. iOS에서 사용할 Apple Developer 계정 정보
3. TestFlight 배포 일정

### 기술적 결정 필요
1. 지도 SDK: Google Maps vs Apple Maps vs 네이버/카카오
2. 음성 입력: 한국어 지원 확인 (speech_to_text)
3. 백그라운드 위치 추적: iOS에서 권한 설정 방법

---

## 버전 정보
- Flutter SDK: C:\src\flutter
- Dart SDK: (Flutter 포함)
- Android Gradle Plugin: 8.1.0
- Kotlin: 1.8.22
- 최소 SDK: 24 (Android 7.0)
- 타겟 SDK: 34 (Android 14)

---

**마지막 업데이트:** 2025-12-30
**작업자:** Claude Code
**다음 세션 시작:** Phase 4 (운행 관리 시스템)
